# AI-DLC Test Registry

This file provides an LLM-discoverable index of every test in the suite. Each entry is extracted from the `# tNN:` comment header in the corresponding source file.

For the full test strategy, layers, fixtures, and assertion guidelines, see [docs/reference/09-testing.md](../docs/reference/09-testing.md).

## Cross-Platform

The suite runs on macOS, Linux, and Windows (via Git Bash for Windows). Validated on Windows Server 2022 for MR 24: L1 passes 1273/1273 and `--all --debug` passes 1516/1516 identically to macOS. Portability conventions used throughout the suite:

- Use `create_test_project` from `lib/fixtures.sh` — it returns `cygpath -m` mixed-mode paths on Windows so JSON encoding and `bun` both work.
- Use `sed_i` helper, not `sed -i` (BSD/GNU incompatible).
- Guard LLM-dependent assertions with `CLAUDE_RC == 124` skip checks — see the `run_claude` docblock in `lib/fixtures.sh` and the t70/t71 examples.
- Avoid `grep -qiF` in favour of `grep -qi` (Git Bash combo-flag bug).

See [docs/reference/09-testing.md § Cross-Platform Coverage](../docs/reference/09-testing.md#cross-platform-coverage) for the full story.

## Test Registry

> **Note:** t19 appears in both unit (`t19-tool-jump.sh`) and integration (`t19-preflight-health.sh`). The integration t19 is the preflight gate; the unit t19 tests the jump CLI tool.

### Smoke (L1)

| Test ID | File | Description |
|---------|------|-------------|
| t01 | `smoke/t01-file-structure.sh` | Verify all expected files exist in dist/claude/.claude/ |
| t02 | `smoke/t02-hook-executability.sh` | All 10 hooks are executable |
| t03 | `smoke/t03-settings-json.sh` | settings.json schema validation |
| t04 | `smoke/t04-shell-lint.sh` | Guard two shell anti-patterns (#34 trailing `[ ] && action`, #41 unbraced `$VAR` + non-ASCII) (2 tests) |
| t05 | `smoke/t05-run-tests-parallel.sh` | Smoke guard for `--parallel N`: invalid values, serial-vs-parallel parity, interleaving, failure propagation, sidecar cleanup, plus `--worktree` tier dispatch under `--ci` (14 tests) |
| t06 | `smoke/t06-claude-md-paths.sh` | Distributable CLAUDE.md describes post-Wave-1 layout — no legacy `practices/`, `aidlc-knowledge/`, or `rules/aidlc/guardrails/` paths; mentions `.claude/sensors/` and `aidlc-org` rule file (6 tests) |
| t86 | `smoke/t86-stage-protocol-section-13.sh` | v0.5.0 MR 4 (+ MR 16) — stage-protocol §13 (Learnings Ritual) prose pin + MEMORY_EMPTY registry placement + the SKILL.md run-stage-gate-branch wiring that makes the orchestrator call the gate; asserts `## 13. Learnings Ritual` heading, four canonical memory.md headings, two-surface learnings + `sensors:` bind (no `applies_to` fossil), MEMORY_EMPTY in three sources (TS / audit-format.md / 12-state-machine.md), the immutability invariant subsection, the SKILL.md gated run-stage branch call to `aidlc-learnings.ts surface` + `persist` (test-run-guarded), and the Test-Run-block §13 skip declaration (7 tests) |
| t119 | `smoke/t119-skill-md-line-budget.sh` | v0.6.0 Wave 2 MR 8 — the orchestrator's `SKILL.md` stays under the Agent Skills 500-line ceiling after the engine-forwarding-loop cutover (was 895 lines, ≈1.8× over). Pins the 500 CEILING deliberately, not the actual landing — a later increment collapses the body further, so a tighter pin would falsely regress. `wc -l` ≤ 500 (1 test) |
| t123 | `smoke/t123-skills-spec-conformance.sh` | v0.6.0 Wave 3 MR 11 (+ MR 13 + MR 14) — the smoke-tier half of the Agent-Skills-spec conformance promise ("smoke + unit"): a fast structural sweep over EVERY shipped skill dir (the unit `t123` owns the same invariants with the full frontmatter parser). Derives the expected set exactly as the unit test does — four base skills + four first-batch scope-runners + the 29 graph-derived stage-runners (initialization excluded) + `aidlc-init` — so the dir-count guard catches an unplanned skill, then per skill asserts: SKILL.md exists, frontmatter `name` == dir, `description` present, NO `hooks:` block (spine in `settings.json`, Fork 2→B), body ≤ 500 lines. Pure bash + bun, no LLM (191 tests: 1 dir-count guard + 5 per skill × 38 skills) |
| t130 | `smoke/t130-scope-runners.sh` | v0.6.0 Wave 3 MR 13 — generated scope-runner skills (structural half). Per first-batch runner (`aidlc-bugfix`, `aidlc-feature`, `aidlc-mvp`, `aidlc-security-patch`): SKILL.md exists, frontmatter `name` == dir, `description` present, NO `hooks:` block (the spine moved to `settings.json`), body ≤ 500 lines, and the shell drives `aidlc-orchestrate next --scope <scope>`. Plus the `aidlc-runner-gen.ts scopes --check` drift guard is clean, a non-batch scope (`refactor`) has no runner (curated subset; the rest run via `--scope`), and dropping a fixture scope file makes the generator emit its runner with no code change (24 tests) |

### Unit (L1)

| Test ID | File | Description |
|---------|------|-------------|
| t04 | `unit/t04-agent-frontmatter.sh` | 11 agents have valid frontmatter (55 tests) |
| t05 | `unit/t05-stage-files.sh` | 32 stages parse to slug + phase matching filename and directory (64 tests: 2 per stage) |
| t06 | `unit/t06-skill-frontmatter.sh` | SKILL.md frontmatter valid (9 tests) |
| t07 | `unit/t07-hook-audit-logger.sh` | Unit tests for audit-logger.ts via bun (16 tests) |
| t08 | `unit/t08-hook-validate-state.sh` | Unit tests for validate-state.ts via bun (14 tests) |
| t09 | `unit/t09-hook-log-subagent.sh` | Unit tests for log-subagent.ts via bun (8 tests) |
| t10 | `unit/t10-hook-session-start.sh` | Unit tests for session-start.ts via bun (17 tests) |
| t11 | `unit/t11-hook-statusline.sh` | Unit tests for aidlc-statusline.ts (62 tests) |
| t13 | `unit/t13-hook-input-robustness.sh` | Adversarial input testing for the framework's stdin-driven hooks (20 tests) |
| t14 | `unit/t14-stage-content-validation.sh` | Validates content structure inside each of 32 stage files (160 tests) |
| t15 | `unit/t15-knowledge-file-inventory.sh` | Validates knowledge file inventory and non-emptiness (~80 tests) |
| t16 | `unit/t16-phase-rules-structure.sh` | Phase rules file structure validation (12 tests) |
| t17 | `unit/t17-tool-state.sh` | Unit tests for aidlc-state.ts CLI tool (83 tests) |
| t18 | `unit/t18-tool-audit.sh` | Unit tests for aidlc-audit.ts CLI tool (13 tests) |
| t19 | `unit/t19-tool-jump.sh` | Unit tests for aidlc-jump.ts CLI tool (16 tests) |
| t20 | `unit/t20-unit-workspace-scanner.sh` | Unit tests for the deterministic workspace scanner + --force semantics inside aidlc-utility init (21 tests) |
| t26 | `unit/t26-delivery-agent-timeline-guardrail.sh` | Content guardrail — aidlc-delivery-agent surface must not use human-timeline framing (velocity, burndown, sprint goals) (dynamic: one assertion per scanned file) |
| t27 | `unit/t27-tool-utility.sh` | Unit tests for aidlc-utility.ts CLI tool (81 tests) |
| t28 | `unit/t28-audit-event-sync.sh` | Validates audit event types are in sync between aidlc-audit.ts and audit-format.md (7 tests) |
| t29 | `unit/t29-hook-sync-statusline.sh` | Unit tests for sync-statusline.ts hook (7 tests) |
| t30 | `unit/t30-hook-session-end.sh` | Unit tests for session-end.ts hook (7 tests) |
| t31 | `unit/t31-tool-log.sh` | Unit tests for aidlc-log.ts (decision, answer subcommands) (21 tests) |
| t33 | `unit/t33-tool-bolt.sh` | Unit tests for aidlc-bolt.ts (start, complete, fail, set-autonomy subcommands) (25 tests) |
| t34 | `unit/t34-tool-error-logged.sh` | Unit tests for ERROR_LOGGED emission via lib.ts emitError() across tool CLIs (11 tests) |
| t35 | `unit/t35-tool-recovery-completed.sh` | Unit tests for RECOVERY_COMPLETED emission via acknowledge-compaction flow (11 tests) |
| t36 | `unit/t36-utility-scope-change.sh` | Unit tests for /aidlc --scope <x> mid-workflow scope change (7 tests) |
| t37 | `unit/t37-utility-doctor-drift.sh` | Unit tests for --doctor drift detection + graph-level checks (23 tests) |
| t38 | `unit/t38-utility-status-gate-awareness.sh` | Unit tests for /aidlc --status gate awareness — who is the blocker (5 tests) |
| t46 | `unit/t46-agent-no-numeric-stage-ids.sh` | Agent files reference stages by slug, not numeric ID — format + semantic checks (22 tests) |
| t60 | `unit/t60-valid-scopes-derived.sh` | Scope validation derives from `.claude/scopes/*.md` presence (MR 12); a dropped fixture-scope `.md` flows through init, scope-change, doctor, and keyword inference + scope-table pick it up from its `.md` frontmatter (9 tests) |
| t61 | `unit/t61-agent-metadata-derived.sh` | Agent display name + examples derive from `.claude/agents/*.md` frontmatter; fixture agent flows through init and statusline (5 tests) |
| t62 | `unit/t62-stage-schema.sh` | Stage frontmatter schema — 13 fields, types, enums, reserved keys, optional agent lookup (59 tests) |
| t63 | `unit/t63-tool-graph.sh` | Unit tests for aidlc-graph.ts `artifactsRegistry()` + `artifacts` CLI — empty graph, union, dedup, CLI surface (14 tests) |
| t64 | `unit/t64-stage-parser.sh` | Unit tests for `parseStageFrontmatter` and `emitStageFrontmatter` — YAML parsing, nested-object lists, round-trip, schema integration (45 tests) |
| t67 | `unit/t67-scope-table.sh` | aidlc-utility `scope-table` subcommand emission (row count matches `.claude/scopes/*.md` count post-MR-12) + `--check` drift guard + `detect-scope --from-text` keyword inference, keywords sourced from `.md` frontmatter (word-boundary matching, multi-word whitespace tolerance, >5-word fallback, flag-collision error, `Matched keywords` audit field) + backward-compat for `--scope` path (28 tests) |
| t68 | `unit/t68-version-changelog-sync.sh` | Validates `AIDLC_VERSION` in `version.ts` agrees with the latest `## [N.N.N]` heading + matching `[N.N.N]:` link reference in `CHANGELOG.md`, that heading-count matches link-ref-count (post-rebase duplicate-heading guard), that `bun aidlc-utility.ts version` prints the same value (CLI wiring assertion), and that the `README.md` shields.io version badge matches `version.ts` (stale public-badge guard) (6 tests) |
| t69 | `unit/t69-worktree-path.sh` | Behavioural contract for `worktreePath(projectDir, boltSlug)` — repo `.gitignore` has the anchored `/.aidlc/` entry, output is absolute when projectDir is absolute, output ends with `/bolt-<slug>`, slug passes through unsanitised (MR 2 ships no validation; deferred to MR 7) (4 tests) |
| t70 | `unit/t70-worktree-kb-and-skill.sh` | v0.4.0 MR 7 KB rewrites + pipeline-deploy agent wiring — `shared/rules-reading.md` exists with empty-template + semantic-topic + fallback sections; `branching-strategies.md` has 5 Execution-runbook + 5 Failure-modes sub-sections + Response contract top-level; aidlc-pipeline-deploy-agent loads `.claude/rules/` at position 4. (The former SKILL.md per-Bolt Steps 0/0.5/6.5/6.75 and halt-and-ask shape assertions were retired at the engine cutover — that orchestration prose moved into the engine; covered now by the t118 differential corpus.) (9 tests) |
| t71 | `unit/t71-markdown-section-helpers.sh` | Behavioural contract for `extractMarkdownSection` / `appendUnderHeading` / `replaceSection` in `lib.ts` — load-bearing for MR 8 practices-discovery cross-row promotion. Pins extract returns prose between heading and next ##, append inserts before next heading and is additive across calls, replaceSection overwrites and is idempotent, all three throw or empty-string on missing heading (12 tests) |
| t72 | `unit/t72-worktree-info.sh` | v0.4.0 MR 12 — deterministic contract for `aidlc-worktree info --slug` halt-and-ask correlation lookup. Pins JSON shape (slug, path, branch_name, audit_timestamp), most-recent-wins on multiple `WORKTREE_CREATED` blocks for same slug, non-zero exit + stderr message on missing slug, non-zero exit + stderr on malformed block (10 tests) |
| t76 | `unit/t76-state-fork-merge.sh` | Behavioural contract for `aidlc-state.ts fork` and `aidlc-state.ts merge` (v0.4.0 MR 9) — fork copies main state to worktree byte-identically (modulo Worktree Path + Bolt Refs), rejects invalid slugs, fails loud on missing worktree dir / v6 state; strict audit-first with both Part A (lock-held → no write) and Part B (post-emit write fail → orphan STATE_FORKED + slug-tagged ERROR_LOGGED for MR 15 reconciliation); merge byte-identical round-trip via cmp -s, workflow-level fields untouched, alphabetical-slug tiebreak as defence-in-depth, idempotent re-run, lock-timeout failure mode, concurrent forks with sorted Bolt Refs; B1 lock-leak / B2 phantom-audit / M1 audit-hash regression tests pin the post-review fold-in (16 tests) |
| t77 | `unit/t77-bolt-worktree-flags.sh` | v0.4.0 MR 11 — `aidlc-bolt.ts` worktree flag branches (`start --worktree`, `complete --merge`, `abort [--discard]`, `fail --slug`). Pins atomicity ordering (`BOLT_STARTED → STATE_FORKED → AUDIT_FORKED`); `--worktree`/`--merge` require `--slug` and reject csv `--name`; failure envelope `{ok:false, slug, stage, reason, detail}` complete (all 5 fields pinned including non-empty detail) for halt-and-ask consumption; abort emits `BOLT_FAILED` with `Reason: aborted` (sub-classifier, no new event type); abort default preserves worktree directory (US-1 AC line 51); existing no-flag paths unchanged (regression guards) (28 tests) |
| t79 | `unit/t79-dispatch-event-validation.sh` | v0.4.0 MR 13 — `aidlc-bolt dispatch-event` subcommand contract for the three pre-registered MERGE_DISPATCH_* audit events. Pins literal-per-branch `appendAuditEntry` calls (t48 invariant); per-variant flag validation (INVOKED requires --practices-excerpt; RETURNED requires --strategy in squash/merge/rebase, --target, --confidence in [0,1], --notes; FALLBACK requires --reason, --defaults); JSON envelope shape `{emitted, slug}`; audit row carries every required schema field per audit-format.md:147-149 (12 tests) |
| t80 | `unit/t80-practices-runtime.sh` | v0.4.0 MR 13 — extractMarkdownSection regex edges (lib.ts:1081) + practices-event --type empty emit. Pins helper behaviour for body-less section, HTML-comments-only body (helper returns literal — caller decides empty), trailing-whitespace headings, sub-heading collision (no false positive); PRACTICES_SECTION_EMPTY emit via aidlc-state.ts practices-event --type empty (the new fourth case for U1 layer-3 fallback advisory); audit row carries Section + Fallback fields; post-merge fold-in: helper ignores `## Heading` lines inside fenced code blocks so teaching examples can't masquerade as live sections (7 tests) |
| t81 | `unit/t81-bolt-plan-override.sh` | v0.4.0 MR 13 — discriminator-field disambiguation for PRACTICES_OVERRIDE (v5 §3a). Pins that practices-event --type override accepts MR 13's full field set (Reason, Practices Stance, Bolt-Plan Marker, Bolt slug); audit row carries the discriminator Reason field; t28 audit count stays at 55 (no new event); MR 8 write-failure path coexists with MR 13 marker-conflict path under the same event name (4 tests) |
| t82 | `unit/t82-hold-merge-invariant.sh` | v0.4.0 MR 13 post-merge fold-in — HOLD-MERGE invariant tooling on aidlc-bolt.ts. Pins `hold-merge --slug` sets Merge-Held: true in the per-Bolt forked state file; `release-merge --slug` clears it; `complete --merge --slug` refuses with non-zero exit and `{ok:false, reason:"merge-held", ...}` envelope when held; refusal does not emit BOLT_COMPLETED; merge proceeds after release-merge clears the marker; hold-merge errors when the Bolt has no per-Bolt forked state file (10 tests) |
| t83 | `unit/t83-doctor-orphan-worktree.sh` | v0.4.0 MR 15 doctor orphan-reconciliation family (Checks 1, 3, 4, 6) — orphan worktrees (active / preserved-by-abort / cleanup-orphan / unmatched), orphan state files (with WORKTREE_DISCARDED carve-out), orphan audit (AUDIT_FORKED-without-disk-state, orphan-delta drift, PRACTICES_OVERRIDE Reason=write-failure-* without follow-up AFFIRMED), bolt-plan-marker-conflict not flagged, MERGE_DISPATCH_INVOKED orphan as advisory pass=true; fail-clean on no-worktrees; post-implementation regression tests pin merged-and-cleaned no-orphan, multi-INVOKED pair-matching, ms-precision AFFIRMED reconciliation, preserved-by-abort sub-classification, unknown-Reason advisory (16 tests) |
| t84 | `unit/t84-doctor-stale-branch.sh` | v0.4.0 MR 15 doctor stale-branch detection (Check 2 per ROADMAP.md:143) — skips silently when not a git repo; passes on clean repo with zero bolt-* branches; flags stale (branch present, worktree dir absent, no terminal audit row); does not flag live (worktree present) or terminated (WORKTREE_MERGED row present) (5 tests) |
| t85 | `unit/t85-doctor-practices-staleness.sh` | v0.4.0 MR 15 doctor practices-staleness check (Check 5 per MR 6 forward-note) — empty/template-default value reads as never-affirmed (informational pass); affirmed within 90 days passes with day count; affirmed beyond 90 days is advisory pass=true; invalid ISO 8601 string flagged readable; missing field treated as never-affirmed; future-dated timestamp produces clock-skew advisory (6 tests) |
| t86 | `unit/t86-sensor-manifest-schema.sh` | v0.5.0 MR 3 + MR 7b — sensor manifest schema for the 4 framework sensors (`aidlc-required-sections`, `aidlc-upstream-coverage`, `aidlc-linter`, `aidlc-type-check`). Pins filename↔id contract, `kind: deterministic` enum, `command:` canonical invocation prefix, `applies_to` ABSENCE (pull authoring removed it), and `default_severity`/`description` presence. Legacy negative-case fixtures under `tests/fixtures/v05-mr3-sensors-dir/` exercise schema rejection for files written under the old shape — unknown-kind, manifests still carrying `applies_to:`, missing `id:` (28 tests) |
| t87 | `unit/t87-stage-compartment-headers.sh` | v0.5.0 MR 5 — every stage file under `aidlc-common/stages/` contains both `^## Sensors$` and `^## Learn$` H2 headings. Sentinel against future stage authors silently omitting compartments (64 tests) |
| t88 | `unit/t88-parse-memory-headings.sh` | v0.5.0 MR 8 — `parseMemoryHeadings()` in `aidlc-lib.ts` counts entries under each of the four canonical §13 H2 headings (Interpretations / Deviations / Tradeoffs / Open questions). Pure function shared by MR 8's runtime-compile, MR 12's gate ritual, and MR 13's lifecycle. Covers empty input, mixed bullets/prose/ISO lines, fenced-code/blockquote/HTML-comment exclusion, non-canonical-heading termination, CRLF/BOM tolerance, exact-match heading strictness (18 tests) |
| t94 | `unit/t94-sensor-fire-hook-unit.sh` | v0.5.0 MR 10 — PostToolUse Write|Edit hook `aidlc-sensor-fire.ts` 12-step flow. Asserts TTY/stdin-parse/empty-path guards, `.aidlc-sensors/` recursion guard, audit + state-existence pre-init guards, Test Run Mode skip (sensor-fire.skipped append), heartbeat ISO-timestamp shape, missing/`none` Current Stage early-exit, missing stage-graph + stage-not-in-graph guards, empty `sensors_applicable` (workspace-scaffold), and G1 lock-in (entries without `matches` do not fire) (18 tests) |
| t96 | `unit/t96-runtime-fragment-primitives.sh` | v0.5.0 MR 11 — `aidlc-runtime fragment-fork` / `fragment-merge` primitives. Asserts fragment-fork happy path (byte-equal copy + JSON envelope + matching source_runtime_graph_hash + source_present:true), source-absent fallback (writes empty graph + source_present:false), one-shot guard (refuses to overwrite existing fragment), worktree-missing guard, slug validation (missing flag + invalid regex), fragment-merge happy path (unlinks + matching pre-unlink hash), idempotent fragment-absent path, slug validation, defensive fragment-absent on missing worktree, --help lists both subcommands, unknown subcommand error path, --project-dir plumbing in spawnSibling-style invocation order (B1 prerequisite) (14 tests) |
| t97 | `unit/t97-learnings-primitives.sh` | v0.5.0 MR 12 — `aidlc-learnings.ts` primitives + the new `parseMemoryEntries` parser. Asserts one-entry-per-counted-line parsing with the `parseMemoryEntries(raw).length === parseMemoryHeadings(raw).total` invariant (single canonical entry, wrapped two-line → two degenerate entries, fenced fake heading skipped, blank skip, missing-heading tolerance, no-`;` tail→summary); subcommand contract (`--help` lists surface+persist, unknown → 2, missing flags → 1, missing project dir → 1); `surface` (empty → no candidates, I/D/T → one candidate each, Open questions parked never candidates, mixed partition, test-run skip, slug-not-Active → 1); `persist` (project/team scope routing + cid marker, two-write sensor bind, framework-tier rejection → 1, free-text `Source: user_addition`/`free_text_<seq>`, idempotent re-run, belt-and-braces recovery, false-negative guard, test-run skip) (32 tests) |
| t100 | `unit/t100-memory-template-lifecycle.sh` | v0.5.0 MR 13 — per-stage `memory.md` template structure + parser-safety + `aidlc-state.ts` advance/approve `memory_path` JSON key. Asserts the template exists at `.claude/knowledge/aidlc-shared/memory-template.md` with the four canonical `## ` H2 headings (exact `HEADING_TO_KEY` strings, lowercase-q `## Open questions`, column 0, no leading/trailing whitespace, no stray heading), a verbatim blockquote ownership header, and single-line HTML-comment seed examples so `parseMemoryHeadings(template)` returns `total === 0` (MEMORY_EMPTY survives) with all four sub-counts 0; appending one real bullet → `interpretations === 1, total === 1`; `advance` JSON carries `memory_path === aidlc-docs/<phase>/<slug>/memory.md` (forward-slash, projectDir-relative) and `approve` inherits it via `handleAdvance` delegation (19 tests) |
| t103 | `unit/t103-doctor-rule-drift-coverage.sh` | v0.5.0 MR 14 — doctor rule-drift + paired-coverage primitives. Asserts `loadRules()` surfaces `frontmatter.pairing` (consumer, not a schema-add) + `validateRuleFrontmatter` rejects bad shapes; `parseRuleHeadings` (private in `aidlc-graph.ts`, surfaced via `RuleFile.headings`) splits `## ` bodies and skips fenced / blockquote / single-line AND multi-line `<!-- -->` comment lines (the `## Corrections` multi-line-comment-only → empty case); `.headings` is the `AIDLC_RULES_DIR` read seam; rule-drift detect (populated org `## Testing Posture` overlap, empty-org / absent-org no-overlap, `*-learnings.md` participation); paired-coverage prefix-strip (`aidlc-` → bare manifest id), unpaired ghost, feedforward-only in X, M-X=0 branch, determinism (19 tests) |
| t110 | `unit/t110-mcp-server-grants.sh` | MCP registry integrity + the inheritance access model — `.mcp.json` exists and is valid JSON; declares exactly the 5 expected public servers (`context7`, `aws-mcp`, `aws-pricing`, `aws-iac`, `aws-serverless`) as the sole top-level key whose value is a non-null object (no 6th entry, no stray sibling key, no array/null shape); `threat-composer-ai` is intentionally absent (deferred); each server's config shape is usable (context7 is `type: http` with a url + `CONTEXT7_API_KEY` header; each aws-* is `command: uvx` with a non-empty args array whose first element is the expected `<pkg>@latest`, and aws-mcp carries `AWS_REGION=us-east-1`); no committed secrets (every credential-position header value is a `${VAR}` placeholder and no high-entropy/literal-key shape appears anywhere); and the inheritance invariant — no agent carries a bare `mcp__<server>` grant token, and every fully-qualified `mcp__<server>__<tool>` entry names a declared server (servers are inherited, not per-agent-granted; bare tokens are no-ops; fully-qualified restriction entries are permitted but must reference a live server). Deterministic, bun-only (no `jq`) (32 tests) |
| t112 | `unit/t112-learnings-distribution-guard.sh` | v0.6.0 MR 0 — `isFrameworkDistributionPath` guard recognises the RELOCATED framework tree after the `aidlc-claude-code/` → `dist/claude/` move. Behavioural (the guard is internal): drives `aidlc-learnings.ts persist` with a sensor-binding selection and asserts a `--project-dir` ending in `dist/claude` is refused (exit 1, no `aidlc-bad.md` scaffolded — the §13 loop must never write a manifest into the framework distribution), while an ordinary project path is accepted (exit 0, manifest lands under the project's own `.claude/sensors`). A dedicated regression guard so a future refactor that breaks the relocated `join("dist","claude",".claude","sensors")` segment recognition fails loudly — the move otherwise breaks it silently (3 tests) |
| t113 | `unit/t113-directive-schema.sh` | v0.6.0 MR 1 — `aidlc-directive.ts` Directive discriminated union + `validateDirective` runtime validator over the 8 engine-emitted kinds (run-stage, dispatch-subagent, invoke-swarm, present-gate, ask, print, error, done). Imports the validator via `bun -e` (runtime, not grep). Asserts a well-formed directive of each kind → VALID, a positive returns the parsed directive as `data`, a per-kind missing-required-field case names the field + kind (run-stage→lead_agent, dispatch-subagent→worker, invoke-swarm→units, present-gate→memory_path, ask→question, print/error→message, done→reason), an unknown kind → specific `unknown kind: "…"` message, an unknown key on a valid run-stage → `unknown key` message, type mismatches (run-stage gate→boolean, support_agents→array; ask question→string), a run-stage mode enum miss, and shape failures (null/array/string → `expected object`). v0.6.0 Wave 2 MR 9 extends `gate` to `boolean | "unresolved"` (the classify-round-trip sentinel) and adds the optional `conductor_persona` string (decision D-E): the sentinel is accepted while any other gate-string is rejected, and `conductor_persona` must be a string when present. Mirrors `t62-stage-schema.sh`; unit tier, no LLM, no state (30 tests) |
| t114 | `unit/t114-orchestrate-next.sh` | v0.6.0 Wave 1 MR 3 — `aidlc-orchestrate.ts next`, the read-only orchestration-engine handler that reads state + the compiled graph and prints exactly one validated directive (JSON) to stdout, mutating nothing. Drives `next` over the existing state fixtures (`state-mid-ideation`, `state-mid-inception`, `state-construction`, `state-brownfield-init-done`). Asserts: an in-flight current stage → a `run-stage` directive carrying the routing fields read off the graph node (stage, lead_agent, gate); the flag-precedence ladder (an invalid `--scope` errors unconditionally even when state supplies a valid scope — Wave-1 audit finding 4; an explicit valid `--scope` beats `AWS_AIDLC_DEFAULT_SCOPE`; valid env beats the default; an invalid env scope as last resort → an `error` directive carrying the verbatim `Unknown scope` text); read-only dispatch (`--status`/`--version` → `print`); the mutually-exclusive `--stage`+`--phase` guard → `error`; that a WITH-STATE `--phase`/`--stage` jump commits via a `print` directive naming `aidlc-jump.ts execute` with the tool-resolved target + direction (the jump is a mutation; `next` stays read-only — re-anchored from the pre-cutover run-stage assertion the release gate caught producing zero state change); and that `gate` is the human-judgement axis (an `execution: ALWAYS` stage like `intent-capture` still emits `gate:true` — gate is not derived from the conditional-inclusion axis); and (Wave 2 MR 8) that the cutover invocation is engine-compatible — SKILL.md's forwarding loop does not wrap `$ARGUMENTS` in a `--args` flag the parser would drop, and a flag-bearing jump reaches the parser rather than falling through to a bare `next`; and (Wave 2 MR 8 follow-up) that `--init --test-run` threads `--test-run` into the emitted `aidlc-utility.ts init` scaffold command while the control without it does not — the birth-init `--test-run` drop that silently stripped the `Test Run Mode: true` state field read by `jump.ts` on resume; and (Wave 2 MR 8 resume leg) that `--test-run` re-entering an EXISTING workflow whose state lacks the `Test Run Mode` field emits a run-then-continue `print` naming `aidlc-utility.ts enable-test-run` (the resume re-stamp the cutover deleted), with three controls — the field-already-present case does NOT re-emit it (the loop advances to run-stage), a plain resume without `--test-run` never emits it, and a `--scope X --test-run` against a differing scope routes to scope-change FIRST (the test-run persist branch sits after Branch 5 and does not shadow it). Table-driven, mirrors `t19-tool-jump.sh`; unit tier, no LLM (27 tests) |
| t115 | `unit/t115-orchestrate-report.sh` | v0.6.0 Wave 1 MR 4 — `aidlc-orchestrate.ts report`, the commit-the-transition engine handler. A thin dispatcher: it shells out to exactly one of `aidlc-state.ts` approve / advance / complete-workflow per the acted stage's gate status (then finality), so the next `next` reads fresh state. Round-trips `next` → act → `report --result <outcome>` → `next` over `state-mid-ideation`, `state-pre-workspace-detection`, `state-init-active`, `state-final-stage`. Asserts: the gated approval path emits GATE_APPROVED then STAGE_COMPLETED then STAGE_STARTED with exactly one STAGE_STARTED (no double-advance after approve); the non-gated advance path emits STAGE_COMPLETED then STAGE_STARTED, with PHASE_COMPLETED + PHASE_VERIFIED + PHASE_STARTED in order at a phase boundary; final completion emits STAGE_COMPLETED + PHASE_COMPLETED + PHASE_VERIFIED + WORKFLOW_COMPLETED with Status=Completed; missing/unknown `--result` and a missing state file → `error` directives; the tool-level advance replay guard report relies on emits zero new events with `replay:true` (not an error) on a double-commit; no orphan audit lock dir after the call. Table-driven, mirrors `t19-tool-jump.sh`; unit tier, no LLM (22 tests) |
| t116 | `unit/t116-directive-path-resolution.sh` | v0.6.0 Wave 1 MR 5 — the `aidlc-orchestrate.ts` emit-time artifact path resolver wired into the `run-stage` directive builder (closes review Major C). The compiled graph stores artifacts as vocabulary NAMES (`produces` bare names, `consumes` `{artifact, required, conditional_on}` objects); the engine resolves them to canonical `aidlc-docs/...` paths and drops `conditional_on` consumes-entries against the workflow's Project Type. Drives the `run-stage` directive builder via the Branch-10 happy path (seed a fixture, pivot Current Stage to the target + mark it in-flight, then bare `next`) over `state-brownfield-feature` (Brownfield, feature scope) and `state-construction` (Greenfield, feature scope). The vehicle was re-anchored at the engine cutover — it used to reach the builder through `next --stage <slug>`, but a WITH-STATE jump now emits a `print` naming `aidlc-jump.ts execute` (a mutation) rather than a run-stage, so it no longer carries produces/consumes; the path-resolution behaviour under test is unchanged, the jump-emits-print contract is pinned by t114/t117/t118. Asserts: `application-design` produces resolve to the non-per-unit shape `aidlc-docs/inception/application-design/<name>.md` (all 5 names); each consume resolves UNDER ITS PRODUCING stage, not the consuming stage (`requirements` → `inception/requirements-analysis/`, the two `conditional_on:brownfield` consumes `architecture` + `component-inventory` → `inception/reverse-engineering/`), and NO consume resolves under `application-design`'s own dir; those two conditional consumes are PRESENT for Brownfield and DROPPED for Greenfield while non-conditional produces still resolve; the per-unit stages `functional-design` / `code-generation` (`for_each: unit-of-work`) inject `{unit-name}` → `aidlc-docs/construction/{unit-name}/<stage>/<name>.md`; and a negative — a non-per-unit stage does NOT get the `construction/{unit-name}/` prefix. Table-driven, mirrors `t19-tool-jump.sh`; unit tier, no LLM (13 tests) |
| t117 | `unit/t117-orchestrate-branches.sh` | v0.6.0 Wave 1 MR 6 — `aidlc-orchestrate.ts next` non-happy-path branches (jump / resume / init / scope-change / config-change / env-scope), porting the remaining `SKILL.md` handlers into the read-only engine as typed directives. Drives `next` over the state fixtures and asserts: forward/backward/redo jump → a `print` naming `aidlc-jump.ts execute` for the tool-resolved target + direction (a WITH-STATE jump is a mutation the conductor commits, so `next` stays read-only — re-anchored at the engine cutover from the pre-cutover run-stage assertion), each direction cross-checked against `aidlc-jump.ts resolve` (the engine delegates direction, not re-deriving it); a SKIP-for-scope jump → `error` carrying the verbatim `is skipped for scope` resolve wording; resume-with-existing-state (two fixtures) → `ask` (the engine never calls AskUserQuestion); the init guard (state exists, no `--force`) → `error` carrying the verbatim `Use --force to reinitialize`; init on a clean workspace → `print` that does NOT create state (mutation stays conductor-side); mutually-exclusive `--stage`+`--phase` → `error` (verbatim); env-scope-invalid (`AWS_AIDLC_DEFAULT_SCOPE=bogus`) → `error` carrying the literal `Invalid AWS_AIDLC_DEFAULT_SCOPE` substring (shelled-out + captured, not reconstructed); scope-change / config-change against existing state → `print` naming the conductor's command; a phase jump → `print` naming execute for the first in-scope stage of the phase; freeform intent with no workflow → `ask` (scope confirmation); and the init-stage jump guard (SKILL.md step 5) → `error` carrying the verbatim `Cannot jump to initialization stages` for `--stage <init>` (state present and no-state) and `--phase initialization`. Every reused handler is a CLI shell-out (jump.ts + utility.ts export no handlers); only the directive types are imported. Table-driven, mirrors `t19-tool-jump.sh`; unit tier, no LLM (24 tests) |
| t118 | `unit/t118-engine-differential.sh` | v0.6.0 Wave 1 MR 7 — the differential corpus (per-scope half), the WAVE CLOSE GATE. Asserts the deterministic engine `aidlc-orchestrate.ts` emits, FOR EACH OF THE 9 SCOPES (`enterprise, feature, mvp, poc, bugfix, refactor, infra, security-patch, workshop`), the same scope-shaped directive the prose orchestrator (`SKILL.md`) produces today — WITH NO MODEL IN THE LOOP (it shells out to `next` over a Scope-swapped `state-initialization-done` fixture and diffs against a FROZEN golden; never calls `run_claude`; vision §5). Per scope: (A) the FINGERPRINT stage (first EXECUTE stage after the 3 bootstrap init stages — `intent-capture` for enterprise/feature/mvp/poc, `reverse-engineering` for bugfix/refactor/security-patch/workshop, `practices-discovery` for infra; derived from `scope-mapping.json` membership, cross-validated against `t56`'s `--stage reverse-engineering --scope bugfix` landing) → re-anchored END-TO-END at the engine cutover through the post-cutover jump-commit loop (Current Stage pivoted to the last init stage so the fingerprint resolves forward): (A1) `next --stage <fp>` emits a `print` naming `execute --target <fp> --direction forward --scope <scope>` (a WITH-STATE jump is a mutation the conductor commits), then (A2) after running that execute and re-running `next`, the engine lands on the fingerprint `run-stage` whose stage + phase + **gate** match the golden (gate IS asserted — every fingerprint gates `gate:true`); (B) a representative SKIP-for-scope stage → an `error` carrying the verbatim `Stage "..." is skipped for scope "<scope>".` resolve wording (negative half — enterprise/feature SKIP nothing). Plus the gate-axis anchor: an initialization stage (`workspace-detection`) → `run-stage` `gate:false` (bootstrap auto-proceed — gate tracks the human-judgement boundary, not the conditional-inclusion axis). Plus the v0.6.0 Wave 2 MR 9 classified-stance anchor: the first Construction Bolt (`functional-design`, no stance recorded) → `run-stage` `gate:"unresolved"` (the sentinel — the third gate value, the practices-derived case the engine defers to the conductor's classify round-trip). Plus a no-state workflow-birth trio (no seeded state — the close-gate coverage hole): `next <known-scope>` → no-state error (recognised as scope, not freeform — Wave-1 audit finding 2), `next <freeform>` → `ask` (scope confirmation), bare `next` → no-state error. Table-driven, mirrors `t19-tool-jump.sh`; unit tier, no LLM, no model (38 tests) |
| t123 | `unit/t123-skills-spec-conformance.sh` | v0.6.0 Wave 3 MR 11 (+ MR 13 + MR 14) — Agent-Skills-spec conformance guard over every shipped skill dir under `dist/claude/.claude/skills/`. In-repo check (the published `skills-ref` validator is not installed; the plan sanctions a vendored equivalent). Discovers the live skill set, asserts it is exactly the four base skills (`aidlc`, `aidlc-outcomes-pack`, `aidlc-replay`, `aidlc-session-cost`) PLUS the four first-batch scope-runners (MR 13 — `aidlc-bugfix`, `aidlc-feature`, `aidlc-mvp`, `aidlc-security-patch`) PLUS the 29 generated stage-runners `aidlc-<slug>` (MR 14 — the stage-runner set DERIVED from the RUNNABLE compiled stages, the 3 bootstrap initialization stages excluded; t129 owns the stage-runner-set/graph equality and t130 the scope-runner drift, this test owns conformance) PLUS the single `aidlc-init` phase wrapper (packages `/aidlc --init`, standing in for the 3 excluded per-init-stage runners), so a future skill landing without a plan update trips the dir-count guard, then per skill asserts: frontmatter `name` equals the directory name, `description` is present and non-empty (handles inline AND folded `>`/`|` block-scalar form), and the `SKILL.md` body is `<= 500` lines. Tolerates the Claude-Code-native frontmatter extensions (`user-invocable`, `argument-hint`) — documented portable-core-plus-extensions, not violations (vision §9). The `aidlc` skill no longer carries a `hooks:` block (moved to `settings.json` by MR 13). Pure node/bun frontmatter parse, no LLM; unit tier (115 tests: 1 dir-count guard + 3 per skill × 38 skills) |
| t124 | `unit/t124-scope-transpose.sh` | v0.6.0 Wave 3 MR 12 — scope-shape transpose. Asserts `transposeScopeGrid` turns per-stage `scopes:` lists into the EXECUTE/SKIP grid (scope columns = sorted union of declared names; a stage naming a scope is EXECUTE under it, others SKIP), that `compile` emits `scope-grid.json` beside `stage-graph.json` through the canonical sole-writer, that `gridJson`/`canonicalScopeGridJson` are byte-stable + deterministic, and that `compile --check` exits 1 on a stale OR missing grid (drift guard) and 0 on a clean tree. Closes with the shipped grid being cell-identical to `subgraphForScope` across all 9 scopes (12 tests) |
| t125 | `unit/t125-scope-files.sh` | v0.6.0 Wave 3 MR 12 — scope files. Asserts the 9 shipped `.claude/scopes/aidlc-*.md` exist with frontmatter `name` == filename stem, `validScopes()` derives from `.md` presence, `loadScopeMetadata`/`loadScopeMapping` read depth/keywords/description (and workshop's `testStrategy`) from the `.md` frontmatter, dropping a new `aidlc-<x>.md` makes `x` a valid scope (init + `detect-scope --from-text` keyword) with no code change, an isolated scopes dir yields exactly its files, and every `scope-grid.json` column has a matching `.md` (10 tests) |
| t129 | `unit/t129-stage-runner-drift.sh` | v0.6.0 Wave 3 MR 14 — stage-runner drift guard: the set of `skills/aidlc-<stage>/` runner dirs is EXACTLY the RUNNABLE compiled stage-slug list (the 29 non-initialization stages; no missing runner, no orphan). The 29 runners are generated from the compiled graph (`aidlc-runner-gen.ts`); the 3 bootstrap initialization stages ship no per-stage runner (no standalone `--single` meaning) — the init phase is packaged as the single `/aidlc-init` wrapper instead. Without this guard a stage added to the graph would silently ship without a runner. Built on the set-equality drift discipline of t28 / t60 / `compile --check`. Asserts: shipped runner set == runnable compiled slug set (pure-bash cross-check); count is 29; the generator's `check` agrees on the shipped tree; the guard catches a deleted runner (exit 1, MISSING) and an orphan runner (exit 1, ORPHAN); regenerating restores sync. Sandbox copy for the divergence cases; unit tier, no LLM (7 tests) |
| t132 | `unit/t132-hooks-doc-count-sync.sh` | v0.6.0 Wave 3 cleanup — doc-count drift guard for the hook-scope sentence in `docs/reference/06-hooks-and-tools.md` (MR 13 corrected "ten hook scripts ... All ten project-wide ... the other nine via the `hooks` block" but pinned no count). Mirrors t28's two-source set-equality discipline: derives ground truth from the `hooks/aidlc-*.ts` files on disk (10) AND the `settings.json` registrations (9 in the `hooks` block + 1 `statusLine` = 10), cross-checks disk == settings total, then asserts the doc's three count-words agree both directions — "uses N" == "All N project-wide" (internal), total == ground truth (forward), "other M via the hooks block" == settings hooks-block count, and M + 1 (statusLine) == N (whole-split, catches a half-correction). A reword that drops the pinned phrasing fails the parse-guard. Pure bash + bun, no LLM (8 tests) |
| t133 | `unit/t133-bolt-dag-compile.sh` | v0.6.0 Wave 4 MR 15 — Bolt-DAG runtime compile + gate-time edge-block sensor. units-generation (2.7) gains a required fenced `yaml` `units:` edge block on `unit-of-work-dependency.md`; `aidlc-runtime compile` parses THAT data (no model call) into a `bolt_dag` node on `runtime-graph.json`, and the `required-sections` sensor validates the same block at the 2.7 gate. Asserts: a valid block yields a `bolt_dag` node with the units and correct sorted topological `batches`; a second compile is byte-identical (pure-data parse, no `Date.now`); a cyclic block and a malformed (dangling-dep) block each OMIT the node with a stderr diagnostic naming the reason; an absent artifact keeps the pre-MR-15 4-key envelope; and the sensor reports `edge_block` `ok`/`cyclic`/`absent` (failing the gate on anything but `ok`) while a non-target markdown file keeps the generic ≥2-H2 check untouched. Pure bash + bun, no LLM (10 tests) |

### Feature (L1)

| Test ID | File | Description |
|---------|------|-------------|
| t12 | `feature/t12-state-fixture-validation.sh` | Meta-test — verify fixture state files match real template structure (20 tests) |
| t30 | `feature/t30-scope-stage-mapping.sh` | Scope-to-Stage Mapping consistency — SKILL.md compiled-table region shape, row count + per-scope EXECUTE count match the compiled `scope-grid.json`, preserved phase-presence semantics (bugfix/workshop have no ideation) (17 tests) |
| t31 | `feature/t31-help-text-consistency.sh` | Help text compiled by `renderHelpText()` from the derived scope mapping (`.claude/scopes/*.md` + grid); verifies every scope appears, stage counts stay fresh, and `feature` row shows `(default)` marker (19 tests) |
| t32 | `feature/t32-stage-graph-consistency.sh` | Cross-reference Stage Graph table in SKILL.md against actual stage files |
| t33 | `feature/t33-hook-concurrency.sh` | Test audit-logger lock contention under parallel writes |
| t34 | `feature/t34-stage-protocol-structure.sh` | Stage protocol structure and cross-reference validation |
| t35 | `feature/t35-stage-protocol-recovery.sh` | Stage protocol recovery and change handling validation |
| t36 | `feature/t36-stage-protocol-governance.sh` | Stage protocol governance and phase boundary validation |
| t37 | `feature/t37-stage-protocol-compliance.sh` | Cross-file protocol compliance — every stage references protocol, state template fields match sed commands, knowledge dirs match agents |
| t38 | `feature/t38-stage-agent-cross-check.sh` | Cross-check Lead Agent in each stage file against Stage Graph table in SKILL.md (32 tests) |
| t39 | `feature/t39-scope-stage-count-validation.sh` | For each of 9 scopes, read EXECUTE counts from the compiled `scope-grid.json` and verify ranges; plus security-patch deployment stages check (9 tests) |
| t39 | `feature/t39-per-scope-phase-sequence.sh` | Per-scope phase sequence test — 9 scopes, data-driven assertions on phase ordering (27 tests) |
| t40 | `feature/t40-settings-hook-config.sh` | Validate settings.json hook configuration (6 tests) |
| t41 | `feature/t41-jump-flag-validation.sh` | SKILL.md forwarding-loop contract + stage-graph data table — engine-cutover rewrite. Was a prose flag-handling-structure check; now asserts the forwarding loop is present and consults the engine (`aidlc-orchestrate next`/`report`/`directive`), the flag-dispatch prose is DELEGATED to the engine (Composable-Flag-Extraction / FORWARD-JUMP / invalid-depth+test-strategy / mutual-exclusivity wording are GONE — pinned by t114/t118), the run-stage gate branch is documented, and the stage-graph data table survives (5 representative slugs). (15 tests) |
| t42 | `feature/t42-state-jumped-fixture.sh` | Validate state-jumped.md fixture structure (12 tests) |
| t43 | `feature/t43-stage-io-contracts.sh` | Stage I/O contract chain — verify inputs/outputs chain between stages (19 tests) |
| t44 | `feature/t44-stage-instruction-completeness.sh` | Stage instruction completeness — verify steps describe how to produce declared outputs (41 tests) |
| t45 | `feature/t45-stage-output-validation.sh` | Feature test — deterministic validation that stage file outputs are referenced in Steps |
| t47 | `feature/t47-construction-bolts.sh` | Construction Bolt-by-Bolt flow — vocabulary, audit events, state field, and Glossary. (The former SKILL.md bolt-plan / walking-skeleton / ladder-prompt assertions were retired at the engine cutover — that per-Bolt orchestration prose moved into the engine.) Pins: "Construction Phase N" labels absent; BOLT_STARTED/COMPLETED/FAILED + AUTONOMY_MODE_SET registered in aidlc-audit.ts; audit-format.md + state-template.md (Construction Autonomy Mode) + stage-protocol Glossary + code-generation gating note (12 tests) |
| t48 | `feature/t48-audit-event-emitters.sh` | Drift test for audit event taxonomy — every emitter-registry row in state-machine.md matches an emission call site (16 tests) |
| t49 | `feature/t49-state-machine-lifecycle.sh` | Feature-tier walk through the full stage lifecycle `[ ] → [-] → [?] → [R] → [?] → [x]` via state-tool commands (14 tests) |
| t52 | `feature/t52-drift-meta-validation.sh` | Meta-test — verify t48 actually catches each failure class it claims to guard (5 tests) |
| t54 | `feature/t54-compaction-and-test-run.sh` | Structural assertions for the `--test-run` terminal state in aidlc-jump.ts (recognises `--test-run`, emits WORKFLOW_COMPLETED with `Reason=test-run-stopped-at-<target>`, end-to-end). (The former SKILL.md compaction-awareness assertions — compaction_pending, Continue/Review/Restart AUQ, acknowledge-compaction — were retired at the engine cutover; the engine's resume `ask` does not yet reproduce the compaction nuance, a tracked engine gap.) (4 tests) |
| t55 | `feature/t55-test-suite-drift.sh` | Drift guard for test-suite metadata — `plan N` vs header `(N tests)` vs `tests/README.md` vs `docs/reference/09-testing.md`, plus path + version-marker drift sweep and the legacy `aidlc-claude-code/` distributable-root guard (v0.6.0 MR 0's permanent post-move residual check) (7 tests) |
| t65 | `feature/t65-stage-file-migration.sh` | End-to-end: all 32 stage files parse + validate against MR 5 schema, produces/consumes integrity, round-trip, topo-sort preserves stage-graph.json numbering (22 tests) |
| t66 | `feature/t66-graph-library.sh` | aidlc-graph.ts 8-function library + CLI — compile round-trip, edge-local invariant, bootstrap error, duplicate-slug detection, schema validation, filename-in-errors, `--check` drift, graph traversal (full + per-scope sub-DAG), plan-identity + walk + firstInScopeStageOfPhase parity across 9 scopes, the MR 12 `AIDLC_GRAPH_RESOLVE=1` cutover-parity net (frontmatter-derived grid == legacy plan, byte-exact ×9), state-file semantics (completed + SKIP + EXECUTE override), circular import, env-seam, validateScope tiers + designer-export bundle + `export --check` drift guard + env-seam over both graph and scope-mapping caches + `rules_in_context` resolution shape, FIELD_ORDER position, env-seam isolation, drift, round-trip, concurrency + `sensors_applicable` resolution shape, FIELD_ORDER position, env-seam isolation, drift, round-trip, resolveSensorsForStage + `withAuditLock` reentrancy + handler-deregistration (97 tests) |
| t75 | `feature/t75-practices-promote.sh` | Behavioural contract for `aidlc-state.ts practices-promote` — happy-path replaces 5 sections in `team.md` and appends rules with date stamps under `## Mandated`/`## Forbidden`, emits `PRACTICES_AFFIRMED` with right fields; fails closed on missing draft (exits non-zero, emits `PRACTICES_OVERRIDE`, target untouched); fails closed on missing target before any write (atomicity rule); partial draft leaves absent sections alone; idempotent on team.md re-run (24 tests) |
| t76 | `feature/t76-halt-and-ask-prose-shape.sh` | v0.4.0 MR 12 + MR 13 — line-anchored prose-shape contract for halt-and-ask across the downstream surfaces. (The SKILL.md half — slug-derivation paragraph, visible-vs-no-worktree subheading carve-out, `Inspecting a paused Bolt` paragraph, and the MR-13 prefix-hash + orphan-BOLT_STARTED italic carve-outs — was retired at the engine cutover; that per-Bolt worktree-dispatch prose moved into the engine.) Pins stage-protocol.md AUQ template carrying `[path]` + `[branch_name]`, Skip/Abort same-line preservation phrasing, branching-strategies.md dirty-tree bullet + cross-link to halt-and-ask block, aidlc-pipeline-deploy-agent.md conflict bullet appended (7 tests) |
| t78 | `feature/t78-bolt-worktree-lifecycle.sh` | v0.4.0 MR 11 — end-to-end Bolt-with-worktree lifecycle (Issue 75 US-1 line 216). start --worktree → simulate per-Unit work → complete --merge round-trips with canonical 6-event audit sequence (`BOLT_STARTED → STATE_FORKED → AUDIT_FORKED → BOLT_COMPLETED → STATE_MERGED → AUDIT_MERGED`); abort --discard with successful discard emits BOLT_FAILED then verifies directory teardown; abort without --discard preserves worktree directory + contents per US-1 AC line 51; two parallel-batch Bolts (alpha+beta) round-trip cleanly without state interference (13 tests) |
| t88 | `feature/t88-compile-rules-in-context.sh` | v0.5.0 MR 7a + MR 7b — strict-additive rule resolution at compile, pull authoring. Walks `.claude/rules/` via `AIDLC_RULES_DIR` env-seam against 5 fixture sub-dirs (org-only, org-team-project, all-four, pairing-feedforward-only, bom-frontmatter); asserts per-stage `rules_in_context` lengths and scopes across the full subset; covers schema acceptance of `pairing: feedforward-only`, schema rejection of invalid `pairing` values, BOM-prefixed frontmatter parsing; verifies `--check` drift detection, round-trip determinism, and zero-rules fallback when the rules dir is missing (11 tests) |
| t89 | `feature/t89-compile-sensors-applicable.sh` | v0.5.0 MR 7b — pull-authoring sensor resolution at compile. Walks `.claude/sensors/` via `AIDLC_SENSORS_DIR` env-seam against 11 fixture sub-dirs (basic-import, matches-passthrough, no-matches, empty-matches, multiple-imports, zero-sensors, unknown-id, duplicate-id, id-filename-mismatch, unknown-kind, unknown-keys-tolerated, bom-frontmatter); asserts per-stage `sensors_applicable` resolution + `matches` passthrough + compile-snapshot semantics + schema rejections + FIELD_ORDER positioning + round-trip determinism + `--check` drift on sensor-manifest edits (22 tests) |
| t90 | `feature/t90-runtime-compile.sh` | v0.5.0 MR 8 — `aidlc-runtime.ts compile` walks audit + memory.md to materialise `runtime-graph.json`. Asserts approved/pending pairing, re-jump → latest STARTED wins, v0.4.0-backfill (missing memory.md → memory_entries:null + no MEMORY_EMPTY), empty memory.md → MEMORY_EMPTY emit, --test-run propagation, byte-equivalent re-compile, missing state.md skip, approved+non-zero entries → no MEMORY_EMPTY (negative case), ISO-second collision deterministic ordering, --init --force re-init → LAST WORKFLOW_STARTED wins + prior-workflow rows filtered, MEMORY_EMPTY re-emit suppression (exactly one per (slug, gate-completion)), re-approve still-empty → fresh emit, schema accepts null `started_at`/`agent` on instance-bearing parent rows (29 tests) |
| t91 | `feature/t91-runtime-compile-hook.sh` | v0.5.0 MR 8 — PostToolUse Bash hook `aidlc-runtime-compile.ts`. Asserts dispatch on GATE_APPROVED in last-3 audit blocks, terminal-WORKFLOW dispatch via WORKFLOW_COMPLETED, gate-start dispatch via STAGE_AWAITING_APPROVAL (so MR 12's gate ritual reads fresh `memory_entries`), command-regex skip, recursion-guard skip (aidlc-runtime.ts standalone AND composite-with-aidlc-state.ts), no-transition skip, TTY/empty-stdin guard, malformed-JSON guard, Test-Run propagation across last-3 blocks (13 tests) |
| t92 | `feature/t92-sensor-fire.sh` | v0.5.0 MR 9 — behavioural contract for `aidlc-sensor.ts fire`. Asserts argv validation (8 invalid shapes → exit 1, no audit emit), PASSED round-trip per sensor (4), FAILED round-trip with per-sensor findings-count derivation (required-sections h2_count, upstream-coverage unreferenced.length, linter errorCount, type-check errors.length), tool-unavailable (exit 127 → SENSOR_PASSED + Note), script-error (exit 2 / bad JSON / detail-write-failed → SENSOR_PASSED + Note), budget override (timeout → SENSOR_BUDGET_OVERRIDE with Cap layer/value/Observed value), concurrency (5-fan-out, lock split across spawn, lock-orphan recovery via process.on("exit")), detail-file body shape + Fire-id-keyed collision-free paths, audit-row required-fields per event type (FIRED/PASSED/FAILED/BUDGET_OVERRIDE), manifest `command:` resolves to per-sensor scripts, validation order (pre-lock failures emit zero SENSOR_FIRED rows), upstream-coverage `--consumes` resolution via `loadGraph()`. Groups B/C/F-defensive use REAL fixtures under `tests/fixtures/v05-mr9-sensor-fire/{passing-markdown,failing-required-sections,failing-upstream-coverage,passing-typescript,failing-linter,failing-type-check,slow-command}/` so the four shipped per-sensor scripts are exercised against authentic markdown + TS inputs (43 tests) |
| t93 | `feature/t93-sensor-list-describe.sh` | v0.5.0 MR 9 (rows updated by MR 10) — behavioural contract for `aidlc-sensor.ts` read-only subcommands. Asserts `list` enumerates the 4 framework sensors in deterministic alpha order (id<TAB>kind<TAB>description, kind=deterministic for all), `describe <id>` for each of the 4 sensors lists canonical fields (markdown sensors include `matches: **/aidlc-docs/**` post-MR-10 G1.5; linter/type-check include `matches: **/*.{ts,js}` and `matches: **/*.{ts,tsx}` respectively), `describe <unknown-id>` exits non-zero with known-ids hint, `--help` / `-h` prints usage covering all 3 subcommands, no subcommand → exit non-zero with usage hint (12 tests) |
| t95 | `feature/t95-sensor-fire-hook-feature.sh` | v0.5.0 MR 10 — end-to-end behaviour for `aidlc-sensor-fire.ts` against a mock dispatcher. Asserts argv shape (`fire <id> --stage <slug> --output-path <path>`), single-entry + multi-entry fire, multi-glob per stage (TS write fires only code sensors / markdown write fires only doc-shape sensors at the same Construction stage), glob-filter selection across mixed entries, subprocess timeout → SIGTERM-recorded `recordHookDrop` (uses a patched-timeout fast-fixture copy of the hook), subprocess exit 1 → dispatcher-exit drop, exit-code contract (no `{decision: block}`), heartbeat mtime advances on sequential calls, `sensor-fire.skipped` accumulates ISO-timestamped lines under Test Run Mode (19 tests) |
| t96 | `feature/t96-runtime-instances-compile.sh` | v0.5.0 MR 11 — `aidlc-runtime compile` `instances[]` populator. Asserts single-Bolt → no instances[] (≥2 detection threshold); 3-Bolt parallel → instances[].length=3 with parent started_at/agent nulled, memory_path kept, sensor_firings:[]; outcome rollup (all approved → approved; any failed → failed; pending mix → pending); alphabetical-by-slug ordering both with already-alpha audit and with shuffled STATE_FORKED Timestamps; determinism (re-compile byte-equivalent); MR-11 contract on every BoltInstance (sensor_firings:[], memory_entries:null, memory_breakdown:null) (10 tests) |
| t98 | `feature/t98-sensor-firings-populator.sh` | v0.5.0 MR 12 — `aidlc-runtime compile` `sensor_firings[]` + `learnings_captured` populators. Asserts fire_id pairing (FIRED+PASSED → passed, FIRED+FAILED → failed+detail_path, FIRED+BUDGET_OVERRIDE → budget-override), orphan handling (closed window → incomplete; open window past 60s → incomplete; open window under 60s → omitted; cutoff from baseline_ts, no wall-clock), 4-parallel interleaved terminals all paired by fire_id, ts-ascending sort, byte-equal re-compile; BoltInstance attribution (single-Bolt → parent, 3-Bolt parallel → per-instance with worktree-scoped firings not double-counted on parent); `learnings_captured` (approved → {from_orchestrator, from_user_addition}, pending → null, instance-bearing parent → null); 4-state enum + fire_id presence invariant (16 tests) |
| t104 | `feature/t104-doctor-rule-drift.sh` | v0.5.0 MR 14 — real `/aidlc --doctor` rule-drift row rendering against `AIDLC_RULES_DIR` fixtures. Asserts the N=1 headline `Rule drift: 1 team/project rule(s) overlap org policy (review for contradiction)`, detail carries file + `## Testing Posture` + the quoted org sentence, the row renders `✓` (advisory — never pushes failed), the N=0 quiet render, the org-absent informational pass, and fixture isolation (the fixture's posture, not the shipped rules', drives N=1 — proving the `.headings` read seam end-to-end) (6 tests) |
| t105 | `feature/t105-doctor-paired-coverage.sh` | v0.5.0 MR 14 — real `/aidlc --doctor` paired-coverage row + the `GUARDRAIL_LOADED` emit pin. Asserts the exact `P/(M-X)` label (`1/2 guardrails paired (1 feedforward-only)`), unpaired-rule detail (`unpaired: … → aidlc-ghost (no stage binds it)`), the `✓` advisory render, a `GUARDRAIL_LOADED` row written to `aidlc-docs/audit.md` with Scope/Path/Rule count fields, and the no-`pairing` M-X=0 branch still emitting `GUARDRAIL_LOADED` (6 tests) |
| t106 | `feature/t106-runtime-summary.sh` | `aidlc-runtime.ts summary` deterministic aggregate over `runtime-graph.json`. Asserts stage-outcome tallies (approved/failed/pending), per-phase rollup keyed by stage-graph phase, memory-entry aggregation by canonical category, `duration_minutes` (start → latest completed; null when pending-only), `--json` determinism across calls, human-readable header render, and missing-graph → exit 1 (13 tests) |
| t107 | `feature/t107-session-skills-readonly.sh` | Read-only session skills (session-cost / replay / outcomes-pack). Asserts each `SKILL.md` exists, declares `name` + `user-invocable: true` + `classification: read-only`, sources its numbers from `aidlc-runtime.ts summary --json`, carries no retired token heuristic (chars÷4), emits no audit / no state advance, and that only outcomes-pack writes a file (24 tests) |
| t111 | `feature/t111-session-skills-contract.sh` | Behavioural contract between the session skills and the `aidlc-runtime.ts summary --json` data plane. Compiles a synthetic graph, then asserts the tool emits the 8 top-level keys the skills' render templates depend on, every dotted field the skills reference resolves in real tool output (catches phantom/renamed fields), and every emitted scalar is consumed by at least one skill (catches a field shipping dark) (10 tests) |
| t118 | `feature/t118-engine-differential.sh` | v0.6.0 Wave 1 MR 7 — the differential corpus (special-path half), the feature-tier complement of the WAVE CLOSE GATE. Walks MULTI-STEP `next`/`report` sequences across the engine's components (next decision rule + report dispatcher) for each SPECIAL PATH the prose orchestrator handles, WITH NO MODEL IN THE LOOP (shells out to the `aidlc-orchestrate.ts` binary over seeded fixtures and diffs FROZEN goldens; never calls `run_claude`; vision §5). Covers all 7 paths (not a subset): jump forward/backward/redo → a `print` naming `aidlc-jump.ts execute` for the resolved target + direction (a WITH-STATE jump is a mutation the conductor commits, re-anchored at the engine cutover from run-stage), each direction cross-checked against `aidlc-jump.ts resolve` (the engine delegates direction; redo has no t50-t59 source — derived from the tool per `t19`); resume → `ask` (the engine never calls AskUserQuestion); init clean → `print` that creates no state, and the state-exists guard → `error` (verbatim `Use --force to reinitialize`); scope-change → `print` naming the scope-change command (no t50-t59 golden — derived from the engine branch); test-run → a full `next` → `report --test-run` → `next` round-trip where the report dispatcher rides `--test-run` to `aidlc-state.ts approve`, stamping `Test-Run: true` on the GATE_APPROVED row (absent in the no-`--test-run` control) and the follow-up `next` reflecting the advanced stage. Plus three cross-component WALKS (`next` → `report` → `next`): a non-gated advance walk (`workspace-detection` `gate:false` → `advance` → `state-init`), a gated approve walk (`feasibility` `gate:true` → `approve` with exactly one STAGE_STARTED → `scope-definition`), and the v0.6.0 Wave 2 MR 9 CLASSIFY round-trip (`functional-design` `gate:"unresolved"` → `report --skeleton-stance on` records the stance with no transition → the follow-up `next` re-emits `functional-design` with the determined `gate:true`). Table-driven, mirrors `t19-tool-jump.sh`; feature tier, no LLM, no model (27 tests) |
| t120 | `feature/t120-classify-roundtrip.sh` | v0.6.0 Wave 2 MR 9 — the walking-skeleton classify round-trip (vision §6, the one knowledge round-trip the engine DEFERS rather than decides; review Major A). The first Construction Bolt's gate depends on the walking-skeleton STANCE, which an LLM classifies from a team's free-form `## Walking Skeleton` prose — so the engine honours the boundary with a round-trip, WITH NO MODEL IN THE LOOP (shells out to `aidlc-orchestrate.ts next`/`report` over the `state-construction-bolt1` fixture; never calls `run_claude`). For each stance (on / off / scope-dependent): step 1 — `next` → `run-stage(functional-design)` with `gate:"unresolved"` (the sentinel; the engine defers the skeleton gate); step 2 — `report --skeleton-stance <s>` → `print` (stance accepted, re-run next) and the `Skeleton Stance` field is written to state; step 3 — the follow-up `next` → `run-stage(functional-design)` with the now-DETERMINED boolean `gate:true` (the determined gate is `true` for every stance per the verified resolution prose — the stance picks the ceremony, not whether Bolt 1 gates). Plus a backward-compat assertion — a non-skeleton construction stage (`nfr-design`) still emits a plain boolean `gate:true`, never the sentinel — and three negative paths (kind + verbatim wording): invalid stance value, stance reported off the skeleton-gate stage, and stance reported with no state file all → `error`. Mirrors `feature/t118-engine-differential.sh`; feature tier, no LLM, no model (19 tests) |
| t121 | `feature/t121-stop-hook-enforce.sh` | v0.6.0 Wave 2 MR 10 — behavioural contract for the Stop hook `aidlc-stop.ts`, the framework's FIRST flow-altering hook (it may emit `{"decision":"block"}`, a sanctioned loop-enforcement contract distinct from the sensor-fire hook's advisory never-block contract). Mirrors `t95`'s harness: runs the real hook with `CLAUDE_PROJECT_DIR` pointed at a temp project carrying a MOCK `aidlc-orchestrate.ts` engine whose directive `kind` is set by `MOCK_KIND`. Asserts (a) a pending `run-stage` directive → stdout `{"decision":"block","reason":...}` where the reason is an on-task continuation that names the pending work and re-feeds the forwarding loop (no override-shaped verbs); (b) a `done` directive → empty stdout + exit 0 (stop allowed, no block); (c) the recursion guard, asserted hardest — the no-progress counter seeded AT the default cap (8) with `stop_hook_active:true` RELEASES the stop, a low-cap (3) no-progress streak flips block→block→RELEASE→RELEASE and stays released, and a stage pivot (progress) RESETS the streak so a healthy loop is never throttled; (d) no-op outside AIDLC — no `aidlc-state.md` → exit 0, no block; plus garbage stdin + an unparseable/non-zero engine fail OPEN (exit 0, never crash, never trap). No test for the human-stop carve-out (SPIKE 1: Stop hooks don't fire on Esc). (13 tests) |
| t127 | `feature/t127-single-stage-invariant.sh` | v0.6.0 Wave 3 MR 14 — the `--single` stage-runner POINTER INVARIANT: a single-stage run NEVER touches the main workflow's `Current Stage`. Over a seeded ACTIVE feature workflow parked at `feasibility`, runs `code-generation` via `--single` and asserts both legs leave the main pointer at `feasibility`: `next --stage code-generation --single` → one `run-stage` directive (with the conductor persona — D-E); `report --single --stage code-generation --result completed` → `done` after committing exactly one STAGE_STARTED + one STAGE_COMPLETED tagged `Workflow: single-stage:code-generation` (audit-only). Plus tool-enforced refusals: `report --single` with no `--stage` → `error` committing nothing; `next --single` with no `--stage` → `error`; an init-stage `--single` → `error`; a SKIP-for-scope stage → verbatim skip wording; `--single --phase` → `error`. Feature tier, no LLM (16 tests) |
| t128 | `feature/t128-custom-runner.sh` | v0.6.0 Wave 3 MR 14 — proves "to add a stage, write a stage file": in a sandbox `.claude/` copy, authors a custom stage file, pre-seeds its `{slug, number, name}` row, recompiles, runs `aidlc-runner-gen.ts write`, and asserts the custom stage gets a spec-conformant `skills/aidlc-<slug>/` runner (name == dir; body drives `next --stage <slug> --single`), the drift guard returns to in-sync, and the custom stage is drivable via `--single` (EXECUTE-injected into a fixture scope via the post-MR-12 `scope-grid.json` + a `.claude/scopes/aidlc-fixture-scope.md` file, mirroring t60). Sandbox-only. Feature tier, no LLM (8 tests) |
| t130 | `feature/t130-scope-runners.sh` | v0.6.0 Wave 3 MR 13 — scope-runners drive the engine with their baked scope (behavioural half). For each first-batch runner (`bugfix`, `feature`, `mvp`, `security-patch`) seeds a real `--test-run` init under that scope, runs the SAME first move the runner shell makes (`aidlc-orchestrate next --scope <scope>`), and asserts the engine resolves a real `run-stage` directive for that scope's first EXECUTE stage (greenfield init: `requirements-analysis` / `intent-capture` / `intent-capture` / `nfr-requirements`) AND bakes the shared conductor persona into the first directive (decision D-E — the runner does not load it by hand). Full drive-to-done is proven per scope by t118; this pins the runner's baked-scope first move + persona delivery. Pure bash + bun, no LLM, no model (12 tests) |
| t131 | `feature/t131-hooks-settings-fire.sh` | v0.6.0 Wave 3 MR 13 — the hooks move (Fork 2→B). Registration half: `settings.json` registers all six workflow-spine hooks (audit-logger + sensor-fire on PostToolUse `Write\|Edit`; sync-statusline on `TaskUpdate`; runtime-compile on `Bash`; validate-state on `PreCompact`; log-subagent on `SubagentStop`) plus the `Stop` hook, the audit/sensor matcher is `Write\|Edit`, and `aidlc/SKILL.md` carries no `hooks:` block. Behaviour half: inside a workflow a Write under `aidlc-docs/` makes audit-logger append an audit row and an `aidlc-state.ts` Bash transition makes runtime-compile emit `runtime-graph.json`; outside any workflow (no state/audit) both self-gate to exit-0 no-ops. Pure bash + bun, no LLM (16 tests) |
| t135 | `feature/t135-invoke-swarm.sh` | v0.6.0 Wave 4 MR 17 — the `invoke-swarm` directive end-to-end, deterministic (no live model). ENGINE half: a Construction project parked at `code-generation` (in-flight) with a `bolt_dag` batch on `runtime-graph.json` — with `Construction Autonomy Mode: autonomous` the engine emits `{"kind":"invoke-swarm","units":[...]}` naming the batch; with the grant gated it falls back to a `run-stage` for code-generation (the swarm is gated on the autonomy grant). REFEREE half: the harness plays the conductor over a real worktree fixture — `prepare` a mixed 2-unit batch, stage only one unit's impl on disk, then `finalize` claiming both (the lying-conductor guard refuses the unstaged unit). Asserts the three batch-level audit events `SWARM_STARTED` (prepare) / `SWARM_COMPLETED` (with converged/failed tally) / `SWARM_BATON_RETURNED` (per failed unit) all land, and `finalize` exits 2 (baton returns). Plus the structural skeleton guard: under `bugfix` scope (where code-generation IS the walking-skeleton gate stage) the swarm never fires even with autonomy granted. Pure bash + bun, no LLM (8 tests) |

### Integration (L2)

| Test ID | File | Description |
|---------|------|-------------|
| t19 | `integration/t19-preflight-health.sh` | Preflight health check — validates Claude CLI before integration tests |
| t20 | `integration/t20-integration-status.sh` | Integration test for /aidlc --status (7 tests) |
| t21 | `integration/t21-integration-init.sh` | Integration test for /aidlc --init — first run (10 tests) |
| t21b | `integration/t21b-integration-init-idempotent.sh` | Integration test for /aidlc --init — --force semantics (6 tests) |
| t22 | `integration/t22-integration-doctor.sh` | Integration test for /aidlc --doctor (10 tests) |
| t23 | `integration/t23-integration-help.sh` | Integration test for /aidlc --help (6 tests) |
| t24 | `integration/t24-integration-stage-jump.sh` | Integration test — forward --stage jump via claude CLI (12 tests) |
| t25 | `integration/t25-integration-phase-jump.sh` | Integration test — backward --phase jump via claude CLI (6 tests) |
| t26 | `integration/t26-integration-backward-jump.sh` | Integration test — backward jump via claude CLI (8 tests) |
| t27 | `integration/t27-integration-depth-override.sh` | Integration test — --depth flag override via claude CLI (7 tests) |
| t28 | `integration/t28-integration-test-strategy.sh` | Integration test — --test-strategy flag via claude CLI (6 tests) |
| t29 | `integration/t29-integration-env-scope.sh` | Integration test — AWS_AIDLC_DEFAULT_SCOPE env var via claude CLI (5 tests) |
| t45 | `integration/t45-revision-loop.sh` | Integration test — gate → reject → revise → gate cycle on a gated stage (10 tests) |
| t46 | `integration/t46-parallel-bolt.sh` | Parallel-bolt concurrency test — 5 processes racing on audit.md (5 tests) |
| t47 | `integration/t47-failure-injection.sh` | Failure-injection test — chaos conditions the state machine must handle without data loss (8 tests) |
| t48 | `integration/t48-runtime-graph-end-to-end.sh` | v0.5.0 MR 8 — runtime-graph compile end-to-end against a real `bugfix` workflow. Asserts pre-approve pending row, post-approve approved row + non-null completed_at, ≥2 rows after first approve, v0.4.0 backfill (missing memory.md → memory_entries:null + no MEMORY_EMPTY emit), byte-equivalent re-compile (10 tests) |
| t49 | `integration/t49-bolt-sensor-failures.sh` | v0.5.0 MR 11 — end-to-end real-tool flow for Bolt fork/merge runtime-graph + parallel instances[] + failure modes. Asserts 3-Bolt parallel batch → instances[auth, cart, pay] alphabetical; sensors are advisory (synthesized SENSOR_FAILED in pay's worktree audit propagates to main on audit-merge but all 3 instances stay outcome:approved); MR-11 contract instances[].sensor_firings:[] under live SENSOR_FAILED; Bolt failure rollup (pay BOLT_FAILED + abort --discard → instance pay:failed + parent:failed); idempotent re-merge (second complete --merge errors at state-merge with 'already merged' before fragment-merge); lock-acquire failure ordering (planted lock-dir + AIDLC_AUDIT_LOCK_RETRIES=1 → complete --merge errors before fragment-merge; fragment file remains for retry); soft-gap closure (fragment-merge fails after audit-merge succeeds via directory-stub at fragment path → BOLT_COMPLETED → STATE_MERGED → AUDIT_MERGED → BOLT_FAILED partial-success audit signature); determinism under failure mix (8 tests) |
| t99 | `integration/t99-learnings-gate-flow.sh` | v0.5.0 MR 12 — §13 learning-gate end-to-end (surface → simulated-AUQ glue → persist). Asserts Case 1 (surface mixed memory → 3 candidates + 1 parked → persist project + team learnings + 2 audit rows); Case 2 (test-run → surface skipped, persist refuses, no ritual rows); Case 3 (sensor proposal → project-tier manifest parses + next `aidlc-graph compile` binds the id into the originating stage's `sensors_applicable` — the two-write install — + `SENSOR_PROPOSED` with `Destinations: [<origin_stage>]`); Case 3b (admission conflict-check with stubbed verdict — rejected entry never reaches persist, user-escalated entry writes through); Case 4 (idempotent re-run → no-op); Case 5 (concurrent persist serialises → exactly one row + line); Case 6 (recovery — audit row present, line gone → re-write only, skip emit, exit 0); glue assertion (label → candidate_id → selection-record fields) + §13 fossil sweep (16 tests) |
| t102 | `integration/t102-memory-roundtrip.sh` | v0.5.0 MR 13 — `memory.md` producer → MR 8 runtime-compile round-trip (scripted, deterministic). Asserts a stage's `memory.md` is created from the real template at start (4 headings, parses to total 0); after N real entries + approval the compile records `memory_entries === N` with a breakdown summing to N (MR 8 reads MR 13's file); a template-only (zero-entry) approved stage emits one `MEMORY_EMPTY` (the signal survives end-to-end) while N≥1 entries emit none; the file persists across a second compile (across sessions); an absent `memory.md` compiles to `memory_entries: null` with no `MEMORY_EMPTY` (no-storm backfill semantics) (6 tests) |

### Stage (L2)

| Test ID | File | Description |
|---------|------|-------------|
| t70 | `stage/t70-stage-workspace-detection-greenfield.sh` | Stage test — workspace detection classifies greenfield stub (8 assertions, 25 turns) |
| t71 | `stage/t71-stage-workspace-detection-brownfield.sh` | Stage test — workspace detection classifies brownfield stub (10 assertions, 25 turns) |
| t72 | `stage/t72-stage-reverse-engineering.sh` | Stage test — reverse engineering on brownfield stub (15 assertions, 25 turns) |
| t73 | `stage/t73-stage-intent-capture.sh` | Stage test — intent capture with greenfield stub (12 assertions, 25 turns) |
| t74 | `stage/t74-stage-requirements-analysis.sh` | Stage test — requirements analysis with brownfield stub + RE artifacts (12 assertions, 25 turns) |
| t101 | `stage/t101-stage-memory-lifecycle.sh` | v0.5.0 MR 13 — per-stage `memory.md` start→approval lifecycle on a gated stage. Asserts init-from-template fired at stage start (file exists, four canonical headings, verbatim blockquote ownership header), any logged observation carries an ISO-8601 prefix under a canonical heading, persist-on-approval (no cleanup), idempotent re-entry (a sentinel entry survives re-running the stage), `parseMemoryHeadings` ↔ disk agreement, and the hook-fired runtime-compile read seam carries `memory_path` on the stage row; LLM-output-dependent assertions skip on timeout or when the orchestrator approximated the copy (8 assertions) |

### Worktree (L2)

| Test ID | File | Description |
|---------|------|-------------|
| t01 | `worktree/t01-helpers.sh` | Meta-test for `tests/lib/worktree-helpers.sh` — fixture creation, worktree assertions, defence-in-depth path guard, idempotent cleanup (7 tests) |
| t02 | `worktree/t02-worktree-create.sh` | `aidlc-worktree create` — happy path emits `WORKTREE_CREATED` and registers worktree on `bolt-<slug>` branch; pre-audit guards reject bad slug, missing base branch, double-create on same slug; 3 parallel creates with distinct slugs all succeed (15 tests) |
| t03 | `worktree/t03-worktree-merge.sh` | `aidlc-worktree merge` — squash strategy emits `WORKTREE_MERGED` and removes the worktree; defensive HEAD check rejects merge from wrong branch; conflict envelope shape (`status`/`detail` + `conflict_files` lists the actual conflicting path via `git diff --diff-filter=U`) with worktree preserved; rebase strategy errors when no remote configured (13 tests) |
| t04 | `worktree/t04-worktree-discard-list-verify.sh` | `aidlc-worktree discard` removes the worktree + branch and emits `WORKTREE_DISCARDED`; idempotent on already-gone slugs; `list` filters to `bolt-*` worktrees only; `verify` exits 0 when the matching event is fresh, non-zero with `absent` reason when missing, non-zero with `stale` reason when older than `--max-age-seconds` (12 tests) |
| t05 | `worktree/t05-worktree-audit-first.sh` | Audit-first invariant — Part A: chmod `audit.md` to read-only, create exits non-zero pre-git with no worktree directory created and no `WORKTREE_CREATED` row in audit.md. Part B: induce git-time failure post-audit (chmod parent dir read-only); `WORKTREE_CREATED` audit-of-intent row landed before git failure, `ERROR_LOGGED` row appended with `[slug=<slug>]` for doctor correlation (7 tests) |
| t06 | `worktree/t06-worktree-sibling-rejection.sh` | `aidlc-worktree` refuses to run from inside a sibling worktree (Construction's `.aidlc/worktrees/bolt-*` are siblings of the main checkout, not nested); error message names the main-checkout requirement (3 tests) |
| t07 | `worktree/t07-audit-fork-merge.sh` | `aidlc-audit audit-fork` / `audit-merge` primitive — fork creates byte-identical worktree audit + AUDIT_FORKED in both audits with matching Fork Boundary; merge appends delta + AUDIT_MERGED in main only; edge cases (empty delta, missing aidlc-docs subdir auto-created, missing main audit fails loud, prefix-hash mismatch refusal, lock contention via 50ms-staggered N=2 mergers, lock-timeout failure path with planted stuck lock, 65-char slug rejected with length error, post-emit failure ERROR_LOGGED carries [fork-emitted:<iso-ts>] for doctor correlation); property — N=4 alphabetical / reverse-alphabetical / same-second-timestamps / one-empty-delta scenarios all preserve per-Bolt fork→merge bracket order in the merged main audit (31 tests) |
| t09 | `worktree/t09-halt-and-ask-preservation.sh` | v0.4.0 MR 12 — halt-and-ask preserves the worktree on simulated Bolt failure. Pins worktree on disk after `BOLT_FAILED`, BOLT_FAILED carries `Bolt slug` field for halt-and-ask correlation, zero `WORKTREE_DISCARDED` events on failure path, info subcommand still resolves path (8 tests) |
| t10 | `worktree/t10-halt-and-ask-discard.sh` | v0.4.0 MR 12 — explicit `aidlc-worktree discard --slug` cleans up: directory gone, branch absent, `WORKTREE_DISCARDED` emitted with `Reason: agent-discard`, idempotent on second discard, info still resolves from audit (audit-of-intent semantics) (8 tests) |
| t11 | `worktree/t11-halt-and-ask-retry-correlation.sh` | v0.4.0 MR 12 — retry-then-fail correlation: `info` returns the SAME path across multiple `BOLT_FAILED` emits for the same slug, exactly one `WORKTREE_CREATED` event (retry re-runs in place, does not re-create), worktree preserved across multiple failures, `Bolt slug` field on every emit (6 tests) |
| t12 | `worktree/t12-bolt-runtime-graph-fork.sh` | v0.5.0 MR 11 — Bolt runtime-graph fork/merge round-trip + parallel batch + abort-discard. Three scenarios: (a) single-Bolt round-trip (start --worktree creates fragment + RUNTIME_GRAPH_FORKED in success-JSON; complete --merge removes fragment + RUNTIME_GRAPH_MERGED in success-JSON; single-Bolt compile produces no instances[] per L5 ≥2 threshold); (b) 3-Bolt parallel batch in non-alphabetical start order, complete in arbitrary user order → main runtime-graph instances[] is alphabetical [auth, cart, pay] regardless of merge order, all 3 fragments removed; (c) abort --discard removes worktree + fragment transitively (no manual fragment-merge); abort without --discard preserves both; manual aidlc-worktree discard cleans up the orphan fragment via git worktree remove (defense-in-depth) (9 tests) |
| t134 | `worktree/t134-swarm-referee.sh` | v0.6.0 Wave 4 MR 16 — `aidlc-swarm.ts`, the stateless convergence **referee** the conductor consults. No headless `claude -p` worker survives the D-G/D-I rework, so there is no binary to swap: the harness plays the conductor, driving the `prepare` / `check` / `finalize` subcommands directly over real git worktrees and staging each worktree's on-disk state the way a worker would (or wouldn't). Asserts: `prepare` forks a worktree per unit and emits `SWARM_STARTED`; `check` returns a stateless verdict (exit 0 only when the real check command passes — never a self-claim — and the same verdict on a repeat call, no counter); editing the protected `--test-file` trips anti-tamper (`tampered:true`, baseline re-derived from the git fork); `finalize` re-verifies every claimed unit before any merge — the **lying-conductor guard** refuses a unit falsely claimed converged (red on disk), lands it in the failure envelope (`SWARM_UNIT_FAILED` + `SWARM_BATON_RETURNED`) and exits 2; a tampered claimed unit is re-verify-rejected at `finalize` too; a mixed batch tallies 1 converged + 1 failed + `SWARM_COMPLETED`; `prepare --degraded-from ultracode` emits `SWARM_DEGRADED`; a `../`-escaping `--test-file` is a typed error, not a silently-disabled guard; and `finalize --reasons <unit>=unsatisfiable` records the conductor's typed attribution on a declined unit while the lying-conductor guard keeps a claimed-but-red unit at `error` (13 tests) |

### Workflow (L3)

| Test ID | File | Description |
|---------|------|-------------|
| t50 | `workflow/t50-workflow-bugfix-scope.sh` | Workflow test — bugfix scope full lifecycle via --test-run (24 tests) |
| t51 | `workflow/t51-workflow-poc-scope.sh` | Workflow test — POC scope via --test-run (19 tests) |
| t51 | `workflow/t51-bugfix-event-parity.sh` | End-to-end event parity test for the bugfix scope — full audit stream vs expected (15 tests) |
| t52 | `workflow/t52-workflow-state-progression.sh` | Workflow test — state file progression during bugfix (10 tests) |
| t53 | `workflow/t53-workflow-scope-routing.sh` | Workflow test — bugfix scope routing (skip Ideation) (11 tests) |
| t54 | `workflow/t54-workflow-audit-completeness.sh` | Workflow test — audit trail completeness during bugfix (10 tests) |
| t55 | `workflow/t55-workflow-init-then-resume.sh` | Workflow test — two-phase: --init then bugfix --test-run resume (8 tests) |
| t56 | `workflow/t56-workflow-forward-jump.sh` | Workflow test — forward jump + auto-init with --test-run (8 tests) |
| t57 | `workflow/t57-workflow-backward-jump.sh` | Workflow test — backward jump resets target + downstream stages with --test-run (5 tests) |
| t58 | `workflow/t58-workflow-workshop-scope.sh` | Workflow test — workshop scope routing (skip Ideation) (14 tests) |
| t59 | `workflow/t59-workflow-depth-override.sh` | Workflow test — depth override persists through bugfix workflow (6 tests) |
| t60 | `workflow/t60-construction-worktrees-enterprise.sh` | Construction-worktrees per-scope contract for enterprise. Pins scope-mapping code-generation EXECUTE; dispatch-event INVOKED + RETURNED bracket the dispatch; practices-discovery EXECUTE; v7 state has the three v0.4.0 fields. The four SKILL.md prose-presence checks (CONSTRUCTION-Flow / practices-preamble / HOLD-MERGE / skeleton-stance) were retired at the engine cutover — that prose was deleted and the behaviour now lives in the engine + aidlc-bolt.ts + stage-protocol.md (see `_construction-worktrees-helpers.sh`) (5 tests) |
| t61 | `workflow/t61-construction-worktrees-feature.sh` | Construction-worktrees per-scope contract for feature (skeleton-on, practices-discovery EXECUTE). Same shape as t60; the four SKILL.md prose-presence checks were retired at the engine cutover (5 tests) |
| t62 | `workflow/t62-construction-worktrees-mvp.sh` | Construction-worktrees per-scope contract for mvp (skeleton-on, practices-discovery EXECUTE). Same shape as t60; the four SKILL.md prose-presence checks were retired at the engine cutover (5 tests) |
| t63 | `workflow/t63-construction-worktrees-poc.sh` | Construction-worktrees per-scope contract for poc (skeleton-on, practices-discovery SKIP). Same shape as t60; the four SKILL.md prose-presence checks were retired at the engine cutover (5 tests) |
| t64 | `workflow/t64-construction-worktrees-workshop.sh` | Construction-worktrees per-scope contract for workshop (multi-engineer parallel scenario). Pins per-scope codegen mode, dispatch-event, and v7 state fields; the four shared SKILL.md prose-presence checks plus the two inline workshop-resume / resume-mid-batch greps were retired at the engine cutover (3 tests) |
| t65 | `workflow/t65-construction-worktrees-bugfix.sh` | Construction-worktrees per-scope contract for bugfix (skeleton-off, practices-discovery SKIP, incremental scope). The four SKILL.md prose-presence checks were retired at the engine cutover (4 tests) |
| t66 | `workflow/t66-construction-worktrees-refactor.sh` | Construction-worktrees per-scope contract for refactor (skeleton-off, practices-discovery SKIP). The four SKILL.md prose-presence checks were retired at the engine cutover (4 tests) |
| t67 | `workflow/t67-construction-worktrees-security-patch.sh` | Construction-worktrees per-scope contract for security-patch (skeleton-off, practices-discovery SKIP). The four SKILL.md prose-presence checks were retired at the engine cutover (4 tests) |
| t122 | `workflow/t122-stop-hook-e2e.sh` | v0.6.0 Wave 2 MR 10 — workflow-tier END-TO-END enforcement of the Stop hook `aidlc-stop.ts`, the framework's FIRST flow-altering hook. Closes the one named-coverage gap left by the feature-tier MOCK test `t121` (which exercises the hook's block/done/guard logic against a MOCK engine): t122 runs the REAL hook against the REAL `aidlc-orchestrate` engine, including one genuinely live `claude -p` pass. A build-time probe confirmed the Stop hook fires under `claude -p` AND that block-and-inject resumes the same headless session — so this IS the live interactive path, not a simulation. Asserts: (e2e) `/aidlc --status` over a COMPLETED workflow (`state-completed`) runs to completion under the live Stop hook with no 124 hang, the session exits 0 reporting the workflow complete (the spec's "the loop runs to `done` under the hook end-to-end"), and — guarded on the durable heartbeat, since the skill-scoped Stop hook does not fire on every `-p` turn — the live hook took the `done`->allow path (`resetGuard` wrote `block-count.json` count 0); (real engine) a PENDING workflow (`state-final-stage`, final stage `[-]`) drives the real engine's real `run-stage` directive and the real hook emits a real `{"decision":"block"}` naming the pending stage + re-feeding the forwarding loop with no override-shaped verbs (the integration `t121`'s mock omits); (real engine) `done` -> empty stdout + exit 0; (real engine) recursion release — a genuinely-pending engine with the no-progress counter seeded AT the cap + `stop_hook_active:true` RELEASES the stop (empty stdout, exit 0, drop record), so a stuck loop never traps the session (light re-confirm; `t121` owns the exhaustive guard corpus). Requires the claude CLI (workflow tier); the human-stop Esc carve-out needs no test (SPIKE 1). (6 tests) |

## Test numbering

When adding a new test, pick the next free integer from the appropriate tier (unit, feature, integration, workflow, worktree). Don't gap-fill skipped numbers from prior MRs — drift-quintet (t55) catches the rename either way, and contiguous numbering keeps the registry readable.

If two parallel MRs both pick the same number, the second to merge rebases and renumbers to the next free integer in the same tier. The drift-quintet enforces five sites that must agree on `(N tests)` after a rename: file header line 1-3, TAP `plan N`, this README row, the `docs/reference/09-testing.md` row, and the README↔09-testing cross-check at `tests/feature/t55-test-suite-drift.sh`. Renaming touches all five.

## Quick Reference

```bash
# Run L1 (protocol — no LLM, seconds)
bash tests/run-tests.sh

# Run L1+L2 (protocol + stage — requires claude CLI)
bash tests/run-tests.sh --ci

# Run all layers (protocol + stage + workflow)
bash tests/run-tests.sh --release

# Run a single tier
bash tests/run-tests.sh --smoke
bash tests/run-tests.sh --unit
bash tests/run-tests.sh --feature
bash tests/run-tests.sh --integration
bash tests/run-tests.sh --stage
bash tests/run-tests.sh --workflow

# Filter by pattern
bash tests/run-tests.sh --integration --filter "t25|t26"

# Run tests concurrently within a tier (LLM tiers benefit most; smoke/unit stay serial)
bash tests/run-tests.sh --all --parallel 4
bash tests/run-tests.sh --integration -P 8

# Verbose / debug output
bash tests/run-tests.sh --verbose
bash tests/run-tests.sh --debug

# Run a single test directly
bash tests/smoke/t01-file-structure.sh
```
